import React from "react";
import { Text, Button } from "react-native-elements";
import { View, StyleSheet } from "react-native";

const GameScreen = () => {
  return (
    <View>
      <Text h1>GameScreen</Text>
    </View>
  );
};

const style = StyleSheet.create({});

export default GameScreen;
