import React from "react";
import { View, StyleSheet, Text, Button } from "react-native";

const WinningScreen = ({ navigation }) => {
  return (
    <View>
      <Text>WinningScreen</Text>
    </View>
  );
};

const style = StyleSheet.create({});

export default WinningScreen;
